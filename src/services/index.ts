/**
 * services/ — orchestration: command bus, regeneration scheduler, dirty
 * tracking, autosave, file import/export flows. May import `core`,
 * `document`, `sketch`, `kernel`. Populated starting M3.
 */
export {};
