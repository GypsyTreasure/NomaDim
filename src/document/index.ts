/**
 * document/ — document model: operations (timeline), sketch data, body
 * metadata, undo/redo transactions, XML codecs, validation. May import only
 * `core` (ARCHITECTURE §3). Populated starting M2/M3.
 */
export {};
